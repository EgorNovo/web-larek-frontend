# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Webpack

Архитектура проекта - MVP

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- src/pages/index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/index.ts — точка входа приложения
- src/styles/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run start
```

и открыть в браузере страницу по адресу http://localhost:8080
## Сборка

```
npm run build
```

## Публикация страницы 
Для публикации страницы на gitHub-pages необходимо выполнить команду

```
"deploy": "gh-pages -d dist"
```

## Описание работы
Исправлю схему после сдачи работы
![UML-схема](<2024-07-07 12.46.24.jpg>)

# Типизация

## Типизация Model
interface IApp  - интерфейс описывающий работу приложения, тут хранится информация о корзине, заказе, каталоге карточек. Методы высчитывают общую стоимость заказа, очищают корзину, создают заказ, заполняют каталог карточек, когда он приходит с api.

```
export interface IApp {
  order: IOrder,
  catalog: ICard[],
  basket: ICard[],
  setCatalog(items: ICard[]):void,
  setPrice():number,
  initOrder():IOrder,
  clearBasket():void
}

```

interface IOrder  - интерфейс описывающий заказ, хранит информацию о карточках в заказе, контактные данные, адрес и способ оплаты. Валидирует импуты, оформляет заказ, очищает заказ.

```
export interface IOrder {
  items: ICard[],
  address:string,
  paymentMethod:paymentMethod,
  email:string,
  phone:string,
  validatePaymentMethod():void,
  validateAddress():void,
  validateMail():void,
  validatePhone():void,
  applyOrder():void,
  clearOrder():void
}

```

## Типизация View

interface IBasketUI  - интерфейс описывающий форму корзины.

```
export interface IBasketUI {
  list: HTMLElement[],
  price: number,
  valid: boolean
}

```

interface ICardUI  - интерфейс описывающий popup с карточкой.

```
export interface ICardUI {
  category: string,
  title: string,
  price: number | null,
  image: string,
  id:string,
  description: string,
  inBasket: boolean
}

```

interface IContantFormUI  - интерфейс описывающий форму с контактной информацией.

```
export interface IContantFormUI {
  email: string
  phone: string,
}

```

interface IPaymentFormUI  - интерфейс описывающий форму с адресом и методом оплаты.

```
export interface IPaymentFormUI {
  paymentMethod: paymentMethod,
  adress: string
}

```

interface ISuccesUI  - интерфейс описывающий popup с сообщение о успешном оформлении заказа.

```
export interface ISuccesUI {
  text: string
}

```
***
***
***

# Модель 
## Классы
### Базовый class Api
Данный класс предназначен для работы с Api, поля класса `baseUri` и `options`.
Методы класса: 
  1) `handleResponse` - обработчик ответа сервера
  2) `get` - делаем запрос на сервер
  3) `post` - отправляем изменения на сервер🤷‍♂️

### Базовый class EventEmmiter
Класс для подписки на события, брокер событий. Поля класса - `_events`.
Методы класса:
  1) `on` - Установить обработчик на событие 
  2) `off` - Снять обработчик с события
  3) `emit` - Инициировать событие с данными
  4) `onAll` - Слушать все события
  5) `offAll` - Сбросить все обработчики
  6) `trigger` - Сделать коллбек триггер, генерирующий событие при вызове 

### Базовый абстрактный class Model
Класс для компонентов модели данных, позволяет связывает Model с presenter. Поля класса - `events`.
Методы класса:
  1) `emitChanges` - Вызов события при изменении данных

### Класс Form
Родительский класс для отображения форм, работает с событием `submit` и изменением `input`. Поля класса - `_buttonSubmit`, `events` и `_errors`.
Методы класса:
  1) `onInputChahge` - Вызов события при изменении данных в инпутах
  2) `render` - расширяет родительский рендер класса `UIComponent`

### Класс Modal
Родительский класс для отображения модальных окон, вставляет верстку шаблонов для нужных окон, обрабатывает события открытия и закрытия окна. Поля класса - `_closeButton` и `_content`.
Методы класса:
  1) `open` - Открывает модальное окно
  2) `close` - Закрывает модальное окно
  3) `handleEscUp` - Закрывает модальное окно при нажатии на кнопку Escape.

### Класс Order
Класс реализующий логику работы заказа, содержит список карточек в заказе, поля - `_paymentMethod`, `_address`, `_email`, `_phone`,  `_items`, `_errors`. Хранит информацию о карточках в заказе, контактные данные, адрес и способ оплаты. Валидирует импуты, оформляет заказ, очищает заказ.
Методы класса: 
 1) `validatePaymentMethod` - валидация способа оплаты.
 2) `validateAddress` - валидация способа адреса.
 3) `validateMail` - валидация способа почты.
 4) `validatePhone` - валидация способа телефона. 
 5) `clearOrder` - очищаем заказ.
 6) `applyOrder` - оформляем заказ.

### Класс App
Класс описывающий работу приложения, тут хранится информация о корзине, заказе, каталоге карточек. Методы высчитывают общую стоимость заказа, очищают корзину, создают заказ, заполняют каталог карточек, когда он приходит с api. Поля - `_order`,`_catalog`,`_basket`, `events`
Методы класса:
1) `setCatalog` - сохраняем каталог карточек
2) `setPrice` - расчитываем итоговую стоимость
3) `initOrder` - иницилизация заказа 
4) `clearBasket` - очищаем корзину 
5) `addInBasket` - добавляем товар в корзину
6) `removeFromBasket` - удаляем товар из корзины
7) `getItemsIdFromBasket` - получаем id карточек в корзине для оформления заказа
8) `createOrderPostData` - формируем объект с нужными полями для post запроса на сервер 

### Базовый абстрактный class UIComponent
Класс для компонентов отвечающих за отображение, позволяет связывает View с presenter. Поля класса -  `container`.
Методы класса:
  1) `setTextConten` - Установить текстовое наполнение 
  2) `toggleClass` - Переключение класса элемента
  3) `setDisabled` - Переключение состояния элемента в верстке
  4) `render` - Рендер элемента

### Класс BasketUI
Класс предаставления, наследуется от UIComponent, нужен для отображения корзины с актуальной информацие о добаленых карточках.
Поля:
  1) `_list` - ссылка на HTMLElement списка, куда будут монтироваься li с описанием карточки 
  2) `_price` - ссылка на HTMLElement параграфа, в нем будет выводиться полная сумма заказа 
  3) `_button` - ссылка на HTMLElement кнопки, через подписку на событие будет запускаться реализация бизнес логики  
  4) `_index` - ссылка на список товаров в корзине
Методы класса: 
 1) `set price` - метод для задания цены заказа в textContent
 2) `set list` - метод для задания элементов списка карточек в корзине
 3) `set valid` - отключаем кнопку, если корзина пустая
 4) `set list` - рисуем карточки в корзине
 5) `renderWithIndex` - расширение родительского метода `render`, задаем порядковые номера для карточек в корзине

 ### Класс CardUI
Класс предаставления, наследуется от UIComponent, нужен для отображения корзины с актуальной информацие о добаленых карточках.
Поля:
  1) `_category` - ссылка на HTMLElement параграфа, в нем будет выводиться категория карточки
  2) `_title`- ссылка на HTMLElement заголовок, в нем будет выводиться заголовок карточки
  3)  `_description`- ссылка на HTMLElement параграфа, в нем будет выводиться описание карточки
  4) `_image`- ссылка на HTMLElement изображения, в нем будет устанавливаться атрибут src
  5) `_price`- ссылка на HTMLElement параграфа, в нем будет выводиться цена карточки
  6) `_button`- ссылка на HTMLElement кнопки, через подписку на событие будет запускаться реализация бизнес логики  
  7) `_buttonDelete`- ссылка на кнопку удаления карточки из корзины
  8) `_id` - id карточки 
  9) `_inBasket` - флаг нахождения картоки в корзине
Методы класса: 
  1) `set category` - метод для задания категории карточки в textContent
  2) `set title` - метод для задания заголовка карточки в textContent
  3) `set price`- метод для задания цены карточки в textContent
  4) `set description` - метод для задания описания карточки в textContent
  5) `set image` - метод для задания атрибута src изображения
  6) `set inBasket`  - выключение кнопки если карточка в корзине
  7) `set id` - задаем id карточки 

 ### Класс ContactFormUI
Класс предаставления, наследуется от UIComponent, нужен для отображения формы с помощью которой получим контактную информацию для заказа.
Поля:
  1) `_mail` - ссылка на HTMLElement input, в нем пользователь задаст контактный адрес почты 
  2) `_phone_`- ссылка на HTMLElement input, в нем пользователь задаст контактный телефон 
Методы класса: 
  1) `set mail` - метод для передачи контактных данных в модель 
  2) `set phone` - метод для передачи контактных данных в модель 

 ### Класс PaymentFormUI
Класс предаставления, наследуется от UIComponent, нужен для отображения формы с помощью которой получим адрес доставки заказа и способ оплаты.
Поля:
  1) `_div` - ссылка на HTMLElement input, в нем пользователь задаст контактный адрес почты 
  2) `_buttons`- ссылка на HTMLElement кнопок, через подписку на событие будет запускаться реализация бизнес логики  
Методы класса: 
  1) `set paymentMethod` - метод для передачи данных о способе оплаты в модель 
  2) `set adress` - метод для передачи данных о адресе доставки в модель 
  3) `setClassMethodSelected` - выделяем стилями выбраную кнопку

 ### Класс SuccesUI
Класс предаставления, наследуется от UIComponent, нужен для отображения popup с информацией о успешном статусе заказ.
Поля:
  1) `_message` - ссылка на HTMLElement параграфа, в нем выводится сообщение о успешном оформлении заказа 
  2) `_button`- ссылка на HTMLElement кнопки, через подписку на событие будет запускаться реализация бизнес логики  
Методы класса: 
  3) `set message` - метод для задания информации о успешном оформлении заказа в textContent

### Класс Page
Класс предаставления, наследуется от UIComponent, нужен для отображения страницы, счетчика корзины.
Поля:
  1) `_counter` - ссылка на HTMLElement счетчика корзины 
  2) `_catalog`- ссылка на HTMLElement кнопки, куда будут монтироваться карточки  
  3) `_basket` - ссылка на иконку корзины
  4) `_wrapper` - блокировка скрола страницы
Методы класса: 
  1) `set catalog` - заполняем каталог 
  2) `set counter` - устанавливаем счетчик корзины
  3) `set block` - блокируем страницу

### Класс LarekAPI
Класс для работы с api, наследуется от Api.
Поля:
  1) `cdn` - часть рабочей ссылки  
Методы класса: 
  1) `getCardItem` - получить карточку с Api
  2) `getCardList` - получить каталог карточек с Api

***
***
***
### Список событий events

`items:chenges`
`card:selected`
`basket:addItem`
`basket:removeItem`
`basket:onChange`
`basket:open`
`payment:open`
`paymentMethod:change`
`order.address:change`
`contacts.email:change`
`contacts.phone:change`
`errors:changed`
`order:submit`
`contacts:submit`
`modal:open`
`modal:close`
`succes:close`

###                                 🤯 